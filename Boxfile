web1:
    type: php
    version: 5.4
    document_root: web
    php_extensions:
        - zip
        - curl
        - apc
    after_build:
        - "curl -s http://getcomposer.org/installer | php"
        - "php composer.phar install --no-progress -o --no-dev --no-plugins"
