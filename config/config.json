{
    "@import": "%root_dir%/config/parameters.json",
    "routing.options" : {
        "cache_dir" : "%root_dir%/cache/routing"
    },

    "twig.options" : {
        "cache" : "%root_dir%/cache/twig"
    },

    "dbs.options" : {
        "bernard" : {
            "driver" : "pdo_sqlite",
            "path" : "%root_dir%/cache/juno.sqlite"
        }
    }
}
